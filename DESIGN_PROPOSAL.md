# MathConnect 升级策划案

> 目标：在保留「连连看 + 数学公式」核心玩法的前提下，从 **视觉表现** 与 **关卡/数学深度** 两个方向升级，让玩家既觉得酷，又能在游玩中真正理解数学之间的联系。

---

## 一、思路 1：让视觉更酷炫

### 1.1 当前问题

- 当前配色偏素，Tile 为纯色卡片 + KaTeX 公式，缺少「能量感」。
- 消除动画仅 500ms 路径线 + 直接变空，反馈较弱。
- 没有主题/氛围包装，数学感有余、游戏感不足。

### 1.2 方向定位

把 MathConnect 包装成 **「数学粒子反应堆」** 或 **「公式星云」** 主题：

- 每个等价无穷小族 = 一种「元素/粒子」。
- 成功连线 = 两条公式共振、坍缩成能量。
- 失败 = 粒子排斥，提示玩家重新寻找配对。

### 1.3 具体方案

#### A. 动态背景与氛围

- **星空/流体背景**：用 CSS 渐变 + 轻量 canvas 粒子，缓慢飘动，营造「公式漂浮在数学空间」的感觉。
- **暗色主题为主**：深蓝/紫色背景 + 霓虹青/粉/金作为不同族的色相区分，对比度高且护眼。
- **响应式光晕**：Tile hover 时产生柔和 glow，选中时 glow 增强并脉动。

#### B. Tile 视觉升级

- **族类颜色编码**：给每个等价类分配一个固定色相（如 `x` 族青色、`2x` 族紫色、`x²` 族金色），Tile 边框/底部渐变条使用该色。
- **玻璃拟态卡片**：半透明背景 + 背景模糊 + 细边框，提升质感。
- **选中态**：卡片轻微上浮 + 外发光 + 公式放大 1.05x。
- **错误态**：快速左右抖动 + 红色闪烁 0.3s。
- **匹配消失态**：
  - 路径先以能量束形式流过（0.4s）。
  - 两个 Tile 同时高亮并内爆缩小，化作粒子向上飘散。
  - 若两个公式不同（如 `\sin x` 与 `\ln(1+x)`），可在消失前 0.2s 短暂显示等号：`sin x = ln(1+x) ~ x`，强化等价认知。

#### C. 连线视觉升级

- 当前是简单 polyline，升级为：
  - **发光路径**：SVG `filter: drop-shadow` + 渐变描边。
  - **粒子沿路径运动**：一个光点从起点沿折线飞向终点。
  - **路径颜色**取两个 Tile 所属族的色相，若为同一族则使用该族色。
  - 拐角处增加小圆点停顿，强调「最多两弯」的规则。

#### D. HUD 与界面动效

- **顶部 HUD**：使用分段进度条 + 剩余 Tile 数，匹配时进度条有微弹跳。
- **连击/ streak 显示**：连续成功消除时显示 `×2`、`×3` 浮层。
- **胜利画面**：清空棋盘后弹出「反应堆稳定」/「公式归位」全屏动画，展示本局用时、步数、准确率。
- **规则面板**：可折叠的侧栏，用图示 + 动画解释「等价无穷小」与「两弯路径」。

#### E. 音效与震动（可选增强）

- 选中：轻微「咔嗒」提示音。
- 匹配成功：上升音阶，随连击升高。
- 错误：低沉嗡鸣。
- 移动/桌面端支持 `navigator.vibrate` 短震动。

### 1.4 实现建议

- 优先使用 CSS 变量定义主题色，方便切换皮肤。
- 动画用 CSS transitions/keyframes 为主，复杂路径粒子可用 SVG + `requestAnimationFrame`。
- 避免引入大型游戏引擎；保持项目轻量（React + Vite + CSS）。

---

## 二、思路 2：让关卡更有趣、更数学

### 2.1 当前问题

- 当前只有一个关卡：等价无穷小（`x -> 0`）。
- 每局只是随机生成 Tile，玩家记住「哪几个公式是一家」即可，缺少对「为什么等价」的理解。
- 没有难度曲线，也没有探索数学联系的成就感。

### 2.2 方向定位

把 MathConnect 从「记忆训练」升级为 **「数学关系探索器」**：

- 每个关卡不再只是「找同类」，而是揭示一类数学关系。
- 消除后给出简短解释，把「配对成功」变成「顿悟时刻」。
- 引入难度分层与解锁机制，让玩家逐步深入。

### 2.3 具体方案

#### A. 多关卡体系（主题 + 难度）

| 关卡名 | 数学主题 | 配对关系 | 教育价值 |
| --- | --- | --- | --- |
| 1. 等价无穷小 | `x -> 0` 时的等价替换 | `sin x ~ x`、`1 - cos x ~ x²/2` 等 | 熟悉常见等价无穷小族 |
| 2. 导数定义 | 函数与其导数定义式 | `f'(x)` 与 `lim_{h->0} [f(x+h)-f(x)]/h` | 理解导数是极限 |
| 3. 积分对偶 | 函数与其反导数 | `f(x)` 与 `F'(x)=f(x)` | 连接微分与积分 |
| 4. 泰勒展开 | 函数与其低阶展开 | `e^x` 与 `1 + x + x²/2` | 理解局部近似 |
| 5. 三角恒等式 | 等价三角公式 | `sin²x + cos²x` 与 `1`、`1 - cos²x` 与 `sin²x` | 强化恒等变形 |
| 6. 对数指数对 | 互逆运算 | `e^{ln x}` 与 `x`、`ln(e^x)` 与 `x` | 理解反函数 |
| 7. 极限结果 | 函数与极限值 | `sin x / x -> 1`、`(1+1/n)^n -> e` | 记忆重要极限 |

每个关卡用同一套连连看机制，但 **classKey 的语义从「等价族」扩展为「数学关系」**。

#### B. 配对后数学解释（核心教育设计）

成功消除后，在路径消失前或 HUD 信息区短暂显示一条「为什么它们能连」的解释：

```
sin x 与 ln(1+x) 都属于 x 族，
因为 lim_{x->0} sin x / x = 1，lim_{x->0} ln(1+x) / x = 1。
```

更长解释可放在「数学笔记」面板，玩家可随时查看。

#### C. 三种子模式（同一关卡内增加变化）

1. **经典模式**：随机棋盘，纯连连看。
2. **引导模式**：首次进入关卡时，Tile 按族分组显示颜色提示，降低学习门槛。
3. **挑战模式**：
   - 限时 60 秒。
   - 每错 3 次扣时间。
   - 加入「干扰项」Tile，它们没有配对，必须用道具或正确路径绕过。

#### D. 数学联系可视化

- **族谱图/关系网**：通关后解锁一张图，显示本关所有公式节点与它们的关系（如 `x`、`sin x`、`tan x`、`e^x-1`、`ln(1+x)` 都指向中心节点 `x`）。
- **公式变形动画**：匹配成功后，1 秒内用 KaTeX 动画把 `e^x - 1` 变形为 `x` 的提示形式，帮助玩家理解等价直觉。

#### E. 自适应难度与生成策略

- 当前 `generateGrid` 是纯随机，可能出现「无法通关」的死局（经典连连看常见问题）。
- 应加入 **可解性预检**：生成棋盘后，检查至少存在一条可连路径；若无，重新洗牌。
- 增加 **难度滑块**：
  - 简单：每族 Tile 数量多、公式差异大。
  - 困难：多族之间公式外形相似（如 `\sin x` 与 `\sin(2x)` 分属不同族），需要更仔细判断。

#### F. 成就与进度系统

- 成就示例：
  - 「等价直觉」：等价无穷小关卡 30 秒内清盘。
  - 「无错大师」：整局零错误。
  - 「关系探索者」：解锁全部 7 个关卡。
- 本地用 `localStorage` 保存进度与最佳成绩。

### 2.4 数据结构调整

当前 `EQUIVALENCE_CLASSES` 可扩展为 `LEVELS`：

```js
export const LEVELS = {
  infinitesimals: {
    title: 'Equivalent Infinitesimals',
    description: 'x -> 0',
    groups: {
      x: {
        label: 'x 族',
        explanation: '这些函数在 x -> 0 时与 x 等价',
        expressions: ['x', '\\sin x', '\\tan x', '...'],
      },
      // ...
    },
  },
  derivatives: {
    // ...
  },
};
```

`checkMatch` 保持按 `classKey` 判断即可，逻辑不变。

---

## 三、两思路的融合点

| 融合设计 | 视觉体现 | 数学体现 |
| --- | --- | --- |
| 粒子坍缩消除动画 | 酷炫、有反馈 | 两个公式「归一」到同一个数学本质 |
| 族类颜色编码 | 界面清晰、美观 | 同一颜色 = 同一数学关系 |
| 关系网/族谱图 | 视觉化成就 | 展示数学结构 |
| 胜利全屏动画 | 爽感 | 显示本关核心公式与解释 |
| 关卡主题背景 | 氛围 | 不同主题对应不同数学领域 |

---

## 四、推荐实施优先级

### 阶段 1：视觉快速升级（1-2 天）

1. 重新定义 CSS 主题变量（暗色 + 族色）。
2. Tile 玻璃拟态 + 选中/错误/匹配动效。
3. 连线发光 + 路径粒子动画。
4. 胜利/清盘画面。

### 阶段 2：数学深度（2-3 天）

1. 重构 `EQUIVALENCE_CLASSES` 为 `LEVELS` 数据结构。
2. 实现关卡选择器。
3. 新增 2-3 个新关卡（如导数定义、泰勒展开、三角恒等式）。
4. 添加配对后解释弹窗/面板。

### 阶段 3：可玩性打磨（2 天）

1. 棋盘可解性预检。
2. 计时/挑战模式。
3. 本地进度与成就。
4. 双语（EN/ZH）规则与解释。

---

## 五、汇报：两大思路的核心价值

- **视觉酷炫化** 不是单纯换皮，而是通过粒子、光效、动效把「数学配对」转化为有反馈的感官体验，降低学习枯燥感，让玩家愿意多玩几局。
- **关卡数学化** 是从单一记忆训练扩展到多维度数学关系探索，每成功一次都是一次「顿悟」；配合解释与关系网，玩家不仅记住公式，更能理解它们之间的联系。
- 两个思路共同指向同一个产品定位：**MathConnect = 一款能让人上瘾的微积分概念探索游戏**。

---

*文档位置：`MathConnect/DESIGN_PROPOSAL.md`*
